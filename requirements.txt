# AppStore 版本信息获取工具依赖库
# 使用以下命令安装所有依赖：
# pip install -r requirements.txt

# 数据可视化库（必需）
matplotlib>=3.5.0

# 网络请求库（可选，用于扩展功能）
requests>=2.25.0

# 进度条显示库（可选，用于更好的用户体验）
tqdm>=4.62.0

# 颜色处理库（可选，用于终端颜色输出）
colorama>=0.4.4

# 开发工具（可选）
# pytest>=6.0.0
# black>=21.0.0
# flake8>=3.9.0
